<section class="training section" id="formation-continue">
	<div class="container training-shell">
		<div class="training-copy">
			<span class="training-kicker">Formation Continue</span>
			<h2>Engagement terrain de DASMARK PHARMA</h2>
			<p>
				A Dasmark Pharma, nous assurons la formations continue de nos d&eacute;l&eacute;gu&eacute;s m&eacute;dicaux
				afin de les rendre plus efficaces et comp&eacute;titifs dans la r&eacute;alisation des objectifs des
				laboratoires.
			</p>
		</div>

		<div class="training-gallery">
			<figure class="training-card">
				<img src="/dasmark.jpeg" alt="Equipe Dasmark Pharma sur le terrain 1" loading="lazy" />
			</figure>
			<figure class="training-card">
				<img src="/dasmarka.jpeg" alt="Equipe Dasmark Pharma sur le terrain 2" loading="lazy" />
			</figure>
			<figure class="training-card">
				<img src="/dasmarkb.jpeg" alt="Equipe Dasmark Pharma sur le terrain 3" loading="lazy" />
			</figure>
			<figure class="training-card">
				<img src="/dasmarkc.jpeg" alt="Equipe Dasmark Pharma sur le terrain 4" loading="lazy" />
			</figure>
		</div>
	</div>
</section>

<style>
	.training {
		padding-top: 1rem;
	}

	.training-shell {
		display: grid;
		gap: 1.2rem;
		padding: 1.3rem 1.2rem 1.6rem;
		border-radius: 3rem;
		background: rgba(255, 255, 255, 0.56);
		border: 1px solid rgba(255, 255, 255, 0.74);
		box-shadow: 0 24px 68px rgba(166, 156, 235, 0.14);
		position: relative;
		overflow: hidden;
	}

	.training-shell::before {
		content: '';
		position: absolute;
		inset: 0;
		background:
			radial-gradient(40% 50% at 10% 20%, rgba(117, 179, 255, 0.15), transparent 70%),
			radial-gradient(36% 46% at 90% 80%, rgba(178, 151, 255, 0.16), transparent 70%);
		pointer-events: none;
	}

	.training-copy,
	.training-gallery {
		position: relative;
		z-index: 1;
	}

	.training-kicker {
		display: inline-flex;
		padding: 0.38rem 0.72rem;
		border-radius: 999px;
		background: rgba(14, 94, 168, 0.1);
		color: #0e5ea8;
		font-size: 0.78rem;
		font-weight: 800;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.training-copy h2 {
		margin: 0.85rem 0 0;
		font-family: 'Plus Jakarta Sans', 'Segoe UI', sans-serif;
		font-size: clamp(1.8rem, 3.2vw, 2.6rem);
		line-height: 1.05;
		letter-spacing: -0.04em;
		color: #30449f;
	}

	.training-copy p {
		margin: 0.9rem 0 0;
		max-width: 72ch;
		color: #66739e;
	}

	.training-gallery {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 0.95rem;
	}

	.training-card {
		margin: 0;
		padding: 0.35rem;
		border-radius: 1.8rem;
		background: rgba(255, 255, 255, 0.72);
		box-shadow: 0 18px 42px rgba(159, 152, 228, 0.14);
	}

	.training-card img {
		display: block;
		width: 100%;
		height: 100%;
		min-height: 210px;
		object-fit: cover;
		border-radius: 1.5rem;
	}

	@media (max-width: 640px) {
		.training-shell {
			padding: 1rem;
			border-radius: 1.6rem;
		}

		.training-gallery {
			grid-template-columns: 1fr;
		}

		.training-card img {
			min-height: 180px;
		}
	}
</style>
