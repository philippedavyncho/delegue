<section class="services section" id="services">
	<div class="container services-shell">
		<div class="services-copy">
			<div class="copy-block">
				<h2>Vision de l'Agence</h2>
				<div class="copy-item">
					<span class="copy-check">✓</span>
					<p>Être les As du Marketing médical et pharmaceutique.</p>
				</div>
			</div>

			<div class="copy-block">
				<h2>Mission</h2>
				<ul>
					<li>Promotion médicale, pharmaceutique et de matériels médicaux.</li>
					<li>Représentation de laboratoires pharmaceutiques.</li>
					<li>Affaires réglementaires.</li>
					<li>
						Formations aux métiers de la délégation médicale.
					</li>
				</ul>
			</div>
		</div>

		<div class="services-gallery">
			<div class="gallery-card gallery-card-large">
				<img src="/bga.jpeg" alt="Réunion de travail Dasmark Pharma" />
			</div>
			<div class="gallery-card">
				<img src="/bgb.jpeg" alt="Échange au bureau" />
			</div>
			<div class="gallery-card">
				<img src="/bgc.jpeg" alt="Présentation en salle de formation" />
			</div>
		</div>
	</div>
</section>

<style>
	.services {
		padding-top: 1.4rem;
	}

	.services-shell {
		display: grid;
		grid-template-columns: minmax(0, 0.92fr) minmax(0, 1.08fr);
		gap: 1.7rem;
		align-items: start;
		padding: 1.2rem 1.2rem 1.6rem;
		border-radius: 3rem;
		background: rgba(255, 255, 255, 0.54);
		border: 1px solid rgba(255, 255, 255, 0.72);
		box-shadow: 0 28px 70px rgba(161, 155, 230, 0.16);
		position: relative;
		overflow: hidden;
	}

	.services-shell::before {
		content: '';
		position: absolute;
		inset: auto 0 -8% 0;
		height: 34%;
		background:
			radial-gradient(80% 100% at 10% 100%, rgba(183, 178, 255, 0.34), transparent 52%),
			radial-gradient(100% 100% at 70% 100%, rgba(153, 203, 255, 0.22), transparent 52%),
			radial-gradient(80% 100% at 100% 100%, rgba(221, 195, 255, 0.26), transparent 56%);
		pointer-events: none;
	}

	.services-copy,
	.services-gallery {
		position: relative;
		z-index: 1;
	}

	.services-copy {
		display: grid;
		gap: 1.1rem;
	}

	.copy-block {
		padding: 1.35rem 1.4rem;
		border-radius: 2rem;
		background: rgba(255, 255, 255, 0.52);
	}

	.copy-block h2 {
		margin: 0 0 1rem;
		font-family: 'Plus Jakarta Sans', 'Segoe UI', sans-serif;
		font-size: clamp(2rem, 4vw, 3rem);
		line-height: 1;
		letter-spacing: -0.05em;
		color: #31469e;
	}

	.copy-item,
	.copy-block li {
		display: flex;
		gap: 0.85rem;
		align-items: flex-start;
		font-size: 1.05rem;
		color: #41507e;
	}

	.copy-item p {
		margin: 0;
	}

	.copy-block ul {
		display: grid;
		gap: 0.85rem;
		padding: 0;
		margin: 0;
		list-style: none;
	}

	.copy-block li::before {
		content: '✓';
		flex: 0 0 auto;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 1.7rem;
		height: 1.7rem;
		border-radius: 999px;
		background: rgba(118, 194, 255, 0.18);
		color: #67b6f4;
		font-weight: 800;
	}

	.copy-check {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 2.2rem;
		height: 2.2rem;
		border-radius: 999px;
		background: linear-gradient(135deg, #8fd1ff, #a49eff);
		color: #fff;
		font-weight: 800;
	}

	.services-gallery {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 0.95rem;
	}

	.gallery-card {
		padding: 0.35rem;
		border-radius: 1.8rem;
		background: rgba(255, 255, 255, 0.72);
		box-shadow: 0 18px 42px rgba(159, 152, 228, 0.14);
	}

	.gallery-card img {
		display: block;
		width: 100%;
		height: 100%;
		min-height: 180px;
		object-fit: cover;
		border-radius: 1.5rem;
	}

	.gallery-card-large {
		grid-column: 1 / -1;
	}

	.gallery-card-large img {
		min-height: 230px;
	}

	@media (max-width: 980px) {
		.services-shell {
			grid-template-columns: 1fr;
			border-radius: 2rem;
		}
	}

	@media (max-width: 640px) {
		.services-shell {
			padding: 0.9rem;
			border-radius: 1.6rem;
		}

		.copy-block {
			padding: 1rem;
		}

		.copy-block h2 {
			font-size: 1.9rem;
		}

		.services-gallery {
			grid-template-columns: 1fr;
		}

		.gallery-card-large {
			grid-column: auto;
		}
	}
</style>
